# コーディングルール

## このファイルについて
AI・開発者がコードを書く際に従うべきルールをまとめる。
命名規則は GLOSSARY.md を参照。

---

## 全般ルール

- コメント・doc コメントは**日本語**で書く
- GLOSSARY.md に定義された用語を使うこと（新用語が発生したら GLOSSARY.md に追記）
- 早すぎる共通化をしない（PACKAGE_RULES.md の共通化判断を参照）

---

## `::UBIQ` ユビキタス言語タグ

### 目的
パッケージ単位（エンドポイント・画面）の分割を超えて、**特定のドメインの概念（ユビキタス言語）がどのソースコードに結びついているか** を特定しやすくするため、ソースコード内に影響タグを埋め込む。

### 形式
```
::UBIQ[用語名]
```

- **1行に1つ**の用語を書く（カンマ区切りは使わない）
- 用語は角括弧 `[]` で囲む
- 記述する用語は、必ず `doc/rules/GLOSSARY.md` に定義されたユビキタス言語と一致させること

### 配置ルール

ファイルの先頭に以下の順序で記述する：

```
① ::UBIQ タグ（1行1つ）
② ファイル説明コメント（1行）
③ 空行
④ コード本体（import / use 等）
```

- `::UBIQ` タグは**ファイルの最上部**に置き、ファイル説明よりも**前**に書く
- `::UBIQ` ブロックとコード本体の間には**空行を1行**入れる
- 新規ファイル作成時に、そのソースが関与するユビキタス言語をタグとして付与する

### 例

```typescript
// ::UBIQ[Domain1]
// ::UBIQ[Domain2]
// [FeatureName]: 〇〇の処理

function doSomething() {
  // ...
}
```

```python
# ::UBIQ[Domain1]
# ::UBIQ[Domain2]
# 〇〇の処理

import os
```

```html
<!-- ::UBIQ[Domain1] -->
<!-- ::UBIQ[Domain2] -->
<!-- 〇〇コンポーネント -->

<div>...</div>
```

### 検索方法（AI・開発者向け）

`-F` は固定文字列モード。`[]` を正規表現として解釈させない。

```bash
# 単一検索
grep -rnF "::UBIQ[Domain1]" src/

# AND検索（Domain1 かつ Domain2 を両方含むファイル）
grep -rlF "::UBIQ[Domain1]" src/ | xargs grep -lF "::UBIQ[Domain2]"

# OR検索（Domain1 または Domain2 を含むファイル）
grep -rnF -e "::UBIQ[Domain1]" -e "::UBIQ[Domain2]" src/
```

---

## 言語・フレームワーク固有のコーディング規約 (プレースホルダー)

### エラーハンドリング
- パッケージ境界を超えて返す場合は明確な型や構造体を使用する
- 想定外と想定内のエラーを区別して扱う

### モジュール構成
- PACKAGE_RULES.md のセクションに従い、必要以上のファイル分割を避ける。
- 状態管理、UIの関心事、型定義などは使用技術に寄せて定義する。
